{
  "name": "ybyra-casting-api",
  "version": "1.0.0",
  "description": "API server that fetches the latest published post from Ghost CMS",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "node-fetch": "^2.7.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}